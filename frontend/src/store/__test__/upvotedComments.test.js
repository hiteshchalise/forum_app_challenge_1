import { addUpvotedComments } from "../user/upvotedComments"
import store from "../store"


describe('it tests upvotedComments slice', () => {

    it('adds upvotedComments in store', () => {

        // store.dispatch(addUpvotedComments())

    })
})